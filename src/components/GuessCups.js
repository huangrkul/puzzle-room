import React, { useEffect, useContext } from 'react';
import { store } from './store.js';

const GuessCups = () => {

  const globalState = useContext(store);

  useEffect(() => {
  })

  return (
    <section>
    </section>
  );
}

export default GuessCups;